name: Build and Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v0.5)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
    - name: Delete existing release
      run: gh release delete ${{ inputs.version }} --yes --cleanup-tag || true
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        GH_REPO: ${{ github.repository }}

  build-windows:
    needs: cleanup
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: Windows-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: Windows-nuget-

    - name: Restore dependencies
      run: dotnet restore

    - name: Test
      run: dotnet test --configuration Release --verbosity normal

    - name: Publish
      run: dotnet publish src/NIS.Desktop/NIS.Desktop.csproj -c Release -r win-x64 -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true --self-contained true -o publish

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SwissNISCalculator-Windows
        path: publish/

  build-linux:
    needs: cleanup
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: Linux-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: Linux-nuget-

    - name: Restore dependencies
      run: dotnet restore

    - name: Test
      run: dotnet test --configuration Release --verbosity normal

    - name: Publish
      run: dotnet publish src/NIS.Desktop/NIS.Desktop.csproj -c Release -r linux-x64 -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true --self-contained true -o publish

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SwissNISCalculator-Linux
        path: publish/

  build-macos:
    needs: cleanup
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: macOS-nuget-${{ hashFiles('**/*.csproj') }}
        restore-keys: macOS-nuget-

    - name: Restore dependencies
      run: dotnet restore

    - name: Test
      run: dotnet test --configuration Release --verbosity normal

    - name: Publish macOS ARM64
      run: dotnet publish src/NIS.Desktop/NIS.Desktop.csproj -c Release -r osx-arm64 --self-contained true -o publish-arm64

    - name: Publish macOS x64
      run: dotnet publish src/NIS.Desktop/NIS.Desktop.csproj -c Release -r osx-x64 --self-contained true -o publish-x64

    - name: Create App Bundle
      run: |
        # Create app bundle structure
        mkdir -p "SwissNISCalculator.app/Contents/MacOS"
        mkdir -p "SwissNISCalculator.app/Contents/Resources"

        # Copy the ARM64 publish output as the base app contents
        rsync -a "publish-arm64/" "SwissNISCalculator.app/Contents/MacOS/"

        # Make executable
        chmod +x "SwissNISCalculator.app/Contents/MacOS/NIS.Desktop"

        # Create Info.plist
        cat > "SwissNISCalculator.app/Contents/Info.plist" << 'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
          <key>CFBundleName</key>
          <string>Swiss NIS Calculator</string>
          <key>CFBundleDisplayName</key>
          <string>Swiss NIS Calculator</string>
          <key>CFBundleExecutable</key>
          <string>NIS.Desktop</string>
          <key>CFBundleIdentifier</key>
          <string>ch.uska.nis-calculator</string>
          <key>CFBundleVersion</key>
          <string>1.0.0</string>
          <key>CFBundleShortVersionString</key>
          <string>1.0.0</string>
          <key>CFBundlePackageType</key>
          <string>APPL</string>
          <key>LSMinimumSystemVersion</key>
          <string>10.15</string>
          <key>NSHighResolutionCapable</key>
        <true/>
        </dict>
        </plist>
        EOF

        # Merge architecture-specific binaries into a single universal binary
        MACOS_CONTENTS="SwissNISCalculator.app/Contents/MacOS"
        find publish-arm64 -type f -print0 | while IFS= read -r -d '' arm_file; do
          rel="${arm_file#publish-arm64/}"
          x64_file="publish-x64/$rel"
          target_file="$MACOS_CONTENTS/$rel"
          if [ ! -f "$x64_file" ]; then
            continue
          fi
          if file "$arm_file" | grep -q "Mach-O"; then
            info="$(lipo -info "$target_file" 2>/dev/null || true)"
            if echo "$info" | grep -q "Non-fat file" || ! (echo "$info" | grep -q "arm64" && echo "$info" | grep -q "x86_64"); then
              lipo -create "$arm_file" "$x64_file" -output "$target_file"
            fi
          fi
        done

    - name: Import signing certificate
      env:
        APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
        APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
      run: |
        if [ -z "$APPLE_CERTIFICATE_BASE64" ] || [ -z "$APPLE_CERTIFICATE_PASSWORD" ]; then
          echo "Signing secrets not configured, skipping code signing"
          exit 0
        fi
        CERT_PATH="$RUNNER_TEMP/macos_cert.p12"
        KEYCHAIN_PATH="$RUNNER_TEMP/app-signing.keychain-db"
        echo "$APPLE_CERTIFICATE_BASE64" | base64 --decode > "$CERT_PATH"
        security create-keychain -p actions "$KEYCHAIN_PATH"
        security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
        security unlock-keychain -p actions "$KEYCHAIN_PATH"
        security import "$CERT_PATH" -k "$KEYCHAIN_PATH" -P "$APPLE_CERTIFICATE_PASSWORD" -T /usr/bin/codesign -T /usr/bin/productsign
        security list-keychains -s "$KEYCHAIN_PATH"
        security default-keychain -s "$KEYCHAIN_PATH"
        security set-key-partition-list -S apple-tool:,apple: -s -k actions "$KEYCHAIN_PATH"

    - name: Sign App Bundle
      env:
        APPLE_CERTIFICATE_BASE64: ${{ secrets.APPLE_CERTIFICATE_BASE64 }}
      run: |
        if [ -z "$APPLE_CERTIFICATE_BASE64" ]; then
          echo "Signing secrets not configured, skipping code signing"
          exit 0
        fi
        # Find the Developer ID Application certificate
        IDENTITY=$(security find-identity -v -p codesigning | grep "Developer ID Application" | head -1 | awk -F'"' '{print $2}')
        if [ -z "$IDENTITY" ]; then
          echo "No Developer ID Application certificate found"
          exit 1
        fi
        echo "Signing with identity: $IDENTITY"
        codesign --force --deep --options runtime --timestamp --sign "$IDENTITY" "SwissNISCalculator.app"
        codesign --verify --deep --strict "SwissNISCalculator.app"

    - name: Notarize App Bundle
      env:
        APPLE_ID: ${{ secrets.APPLE_ID }}
        APPLE_APP_PASSWORD: ${{ secrets.APPLE_APP_PASSWORD }}
        APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      run: |
        if [ -z "$APPLE_ID" ] || [ -z "$APPLE_APP_PASSWORD" ] || [ -z "$APPLE_TEAM_ID" ]; then
          echo "Notarization credentials not configured, skipping"
          exit 0
        fi

        # Create zip for notarization
        ditto -c -k --keepParent "SwissNISCalculator.app" "notarize.zip"

        # Submit for notarization and wait
        xcrun notarytool submit "notarize.zip" \
          --apple-id "$APPLE_ID" \
          --password "$APPLE_APP_PASSWORD" \
          --team-id "$APPLE_TEAM_ID" \
          --wait

        # Staple the notarization ticket
        xcrun stapler staple "SwissNISCalculator.app"

        # Clean up
        rm "notarize.zip"

    - name: Package App Bundle
      run: |
        # Create compressed DMG so users can download a single file and run/install
        hdiutil create -volname "Swiss NIS Calculator" -srcfolder "SwissNISCalculator.app" -ov -format UDZO "SwissNISCalculator-macOS.dmg"

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SwissNISCalculator-macOS
        path: SwissNISCalculator-macOS.dmg

  release:
    needs: [build-windows, build-linux, build-macos]
    runs-on: ubuntu-latest

    steps:
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: SwissNISCalculator-Windows
        path: windows

    - name: Download macOS artifact
      uses: actions/download-artifact@v4
      with:
        name: SwissNISCalculator-macOS
        path: macos

    - name: Download Linux artifact
      uses: actions/download-artifact@v4
      with:
        name: SwissNISCalculator-Linux
        path: linux

    - name: Create archives
      run: |
        cd windows && zip -r ../SwissNISCalculator-Windows.zip . && cd ..
        # macOS DMG already created, just move it
        mv macos/SwissNISCalculator-macOS.dmg .
        cd linux && tar -czvf ../SwissNISCalculator-Linux.tar.gz . && cd ..

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ inputs.version }}
        name: Release ${{ inputs.version }}
        files: |
          SwissNISCalculator-Windows.zip
          SwissNISCalculator-macOS.dmg
          SwissNISCalculator-Linux.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
